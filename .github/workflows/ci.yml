name: CI
on:
  push:
    branches: [main]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test_image
        uses: actions/checkout@v4
      - name: Test_text
        run: echo "Hello world!"
